This is the template to provision Windows server using Ansible.
====================

Steps:

1. Add your own AWS credentials into vars/aws-creds.yml
2. Edit vars/dev-environment.yml to define the security groups, sets of instances, etc。
3. Run: ansible-playbook -i hosts provision.yml

